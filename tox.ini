[tox]
isolated_build = true
envlist = py38

[testenv]
deps = poetry==1.0.5
commands=
    poetry install
    poetry run pytest --cov=rchat tests {posargs}

[testenv:package]
deps = pex
commands = pex . -o rchat.pex -c rchat
